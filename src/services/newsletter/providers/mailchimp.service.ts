import { NewsletterInterface } from "@github20k/services/newsletter/newsletter.interface";

export class MailchimpService implements NewsletterInterface {
  registerToNewsletter(email: string, name?: string) {}
}
